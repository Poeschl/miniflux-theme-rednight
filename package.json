{
  "name": "miniflux-theme-rednight",
  "version": "1.0.0",
  "description": "Redisch midnight theme for Miniflux",
  "author": "Markus Pöschl (GitHub: Poeschl)",
  "scripts": {
    "lint": "stylelint **/*.scss",
    "build": "sass --style compressed --no-source-map style.scss rednight-style.css",
    "dev": "sass --no-source-map --watch style.scss rednight-style.css"
  },
  "devDependencies": {
    "sass": "^1.58.3",
    "stylelint": "^15.2.0",
    "stylelint-config-standard": "^30.0.1",
    "stylelint-config-standard-scss": "^7.0.1"
  }
}
